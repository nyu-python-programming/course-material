
def main():
    # an infinite loop....
    # the loop will keep iterating as long as the boolean expression is True
    # in this case, it's always True because we wrote the True boolean literal
    while True:
        print("Hello")
    
    # this will run whenever the loop terminates
    print("Done!")

# call the main function
main()
